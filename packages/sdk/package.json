{
  "name": "@cgpu-max/sdk",
  "version": "0.1.0",
  "description": "Official Node.js client for the CGPU-MAX public API",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "@cgpu-max/config": "workspace:*",
    "@cgpu-max/types": "workspace:*",
    "typescript": "^5.4.0"
  }
}
